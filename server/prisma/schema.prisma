generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Account {
  id           String @id @default(uuid())
  login        String @unique
  passwordHash String
  refreshToken String @default("")
}
