{
  "name": "rus-vs-lizards-monorepo",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "rebuild": "cd server && npm run build && cd ../client && npm run build && pm2 restart all"
  }
}